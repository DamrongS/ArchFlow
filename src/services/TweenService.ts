import { TweenInfo } from "./TweenInfo";

export class TweenService {
    constructor(what: number, to: number, TweenInfo : TweenInfo) {}

    static play() {}

    static linear(number: number): number {
        return number;
    }
}